name: a2a-platform
kind: platform
version: 1.0.0
description: >
  A2A v1 protocol adapter for Hermes Agent.
  Exposes Hermes as a discoverable A2A agent via JSON-RPC 2.0.
  Agent Card at /.well-known/agent-card.json, JSON-RPC at /a2a.
author: hermes-claw
requires_env:
  - A2A_ENABLED
